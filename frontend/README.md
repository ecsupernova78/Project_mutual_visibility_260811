# Frontend workspace

This directory is reserved for a React, TypeScript, and Vite single-page application.

Only the package boundary is defined at this stage. No UI scaffold or application source has been created yet. When implementation begins, initialize the source tree here without replacing the repository root or `references_2026`.
